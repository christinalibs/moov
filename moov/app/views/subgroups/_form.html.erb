<%= form_for(@subgroup) do |f| %>
  <% if @subgroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subgroup.errors.count, "error") %> prohibited this subgroup from being saved:</h2>

      <ul>
      <% @subgroup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
