<%= form_for(@volunteer) do |f| %>
  <% if @volunteer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@volunteer.errors.count, "error") %> prohibited this volunteer from being saved:</h2>

      <ul>
      <% @volunteer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :bday %><br>
    <%= f.date_select :bday, { start_year: Date.today.year, end_year: Date.today.year - 100, order: [:month, :day, :year] } %>
  </div>
  <!--
  <div class="field">
    <#%= f.label :approved? %><br>
    <#%= f.check_box :approved? %>
  </div>
  <div class="field">
    <#%= f.label :leader? %><br>
    <#%= f.check_box :leader? %>
  </div>
  <div class="field">
    <#%= f.label :admin? %><br>
    <#%= f.check_box :admin? %>
  </div>
  -->
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
