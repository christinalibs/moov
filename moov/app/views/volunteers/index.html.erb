<h1>Listing volunteers</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Bday</th>
      <th>Approved?</th>
      <th>Leader?</th>
      <th>Admin?</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @volunteers.each do |volunteer| %>
      <tr>
        <td><%= volunteer.name %></td>
        <td><%= volunteer.bday %></td>
        <td><%= volunteer.approved? %></td>
        <td><%= volunteer.leader? %></td>
        <td><%= volunteer.admin? %></td>
        <td><%= volunteer.email %></td>
        <td><%= link_to 'Show', volunteer %></td>
        <td><%= link_to 'Edit', edit_volunteer_path(volunteer) %></td>
        <td><%= link_to 'Destroy', volunteer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Volunteer', new_volunteer_path %>
